apiVersion: v1
kind: Service
metadata:
  name: {{ include "assessment-api.fullname" . }}
  labels:
    {{- include "assessment-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assessment-api.selectorLabels" . | nindent 4 }}
